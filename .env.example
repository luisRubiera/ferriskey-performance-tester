# FerrisKey Performance Tester - Minimal Configuration
#
# The seeder auto-generates CLIENT_ID and retrieves CLIENT_SECRET from the server.
# After seeding, use the generated .env.test file for k6 tests.

# IAM provider: "ferriskey" or "keycloak"
IAM_PROVIDER=keycloak

# Server URL
BASE_URL=http://localhost:8080

# Admin credentials (for seeding)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# FerrisKey only: Admin client credentials (existing client in master realm)
# Not needed for Keycloak (uses admin-cli by default)
# ADMIN_CLIENT_ID=master-realm
# ADMIN_CLIENT_SECRET=your-secret
